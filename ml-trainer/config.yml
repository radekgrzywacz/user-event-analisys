database:
  url: postgresql://postgres:postgres@localhost:5432/user_event_analysis_db

training:
  epochs: 10
  batch_size: 256
  learning_rate: 0.001
  embedding_dim: 8
  hidden_dim: 32
  test_split: 0.2
  model_dir: ../models
